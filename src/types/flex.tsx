export type FlexWraperProps = {
  direction?: string,
  justify?: string,
  align?: string,
  fullheight?: boolean;
}

export type FlexItemProps = {
  alignSelf?: string,
  children?: React.ReactNode;
}